# <Feature Name> — Design

## Overview

<One or two lines recapping the goal. Link/point to the plan file this design is
based on: `plans/<feature>.md` or `docs/plans/<feature>.md`.>

## API endpoints

<For each new/changed endpoint. Plain-English types only — no code, no JSON blocks.>

### `<METHOD> <path>`
- **Purpose:**
- **Request fields:** name — type — required/optional — notes
- **Response fields:** name — type — notes

## Data model

<For each new/changed entity. Plain-English types, relationships described in
prose — no schema syntax, no ORM/class definitions.>

### `<Entity name>`
- **Fields:** name — type — notes
- **Relationships:**

## UI layout

<Screens/components affected or newly needed, and the states each should handle
(loading, empty, error, permissions). Prose/bullets only — no JSX or markup.>

- 

## Files to touch

<Real paths from the repo, each with a one-line conceptual description of the
change. No code, no diffs.>

- `<path>` — 

## Open questions / risks

<Anything unresolved or risky heading into implementation. Also note here if the
deeper scan revealed the plan's "Affected areas" were incomplete or wrong.>

- 
